# Python Performance Patterns

- Generators for large data
- `itertools` for efficient iteration
- `functools.lru_cache` for expensive idempotent functions
- `collections.defaultdict` over manual key checks
- `str.join()` for string concatenation in loops
- `set` for membership testing
- `heapq` for priority queues
- `Counter` for counting hashable objects
